# Landing Site i18n Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add proper Astro i18n to the landing site — all user-facing text translatable, EN default (no prefix), VI at `/vi/`, extensible to more locales.

**Architecture:** Astro built-in `i18n` config for routing + Recipe pattern (`src/i18n/ui.ts` + `utils.ts`) for UI translations. Shared page components avoid duplicating logic between locale page files. Content collections (commands, guides) already locale-ready; add `pages` collection for privacy/terms Markdown.

**Tech Stack:** Astro 6, TypeScript, static output

**Spec:** `docs/superpowers/specs/2026-04-09-landing-i18n-design.md`

---

### Task 1: Astro i18n Config + Translation Layer

**Files:**
- Modify: `landing/astro.config.mjs`
- Create: `landing/src/i18n/ui.ts`
- Create: `landing/src/i18n/utils.ts`

- [ ] **Step 1: Add i18n config to astro.config.mjs**

```js
import { defineConfig } from "astro/config";

export default defineConfig({
  i18n: {
    defaultLocale: "en",
    locales: ["en", "vi"],
    routing: {
      prefixDefaultLocale: false,
    },
  },
  output: "static",
  build: {
    assets: "_assets",
  },
  vite: {
    css: {
      transformer: "lightningcss",
    },
  },
});
```

- [ ] **Step 2: Create `src/i18n/ui.ts` with all translation keys**

This file contains all UI string translations for both EN and VI. The `as const` assertion provides full type safety — TypeScript will autocomplete keys and catch typos.

```ts
export const languages = {
  en: "English",
  vi: "Tiếng Việt",
} as const;

export const defaultLang = "en" as const;
export const showDefaultLang = false;

export const ui = {
  en: {
    // Meta
    "meta.description": "Discord bot for voice channel management, manga reading, translation, and more.",

    // Navigation
    "nav.features": "Features",
    "nav.commands": "Commands",
    "nav.guide": "Guide",
    "nav.faq": "FAQ",
    "nav.support": "Support",
    "nav.add": "Add to Server",

    // Hero
    "hero.subtitle": "A Discord bot packed with XP leveling, economy, voice management, manga reader, and 15-language support. Slash commands only, always up.",
    "hero.add": "Add to Server",
    "hero.commands": "View Commands",
    "hero.trust": "Free to use · Slash commands · Online since 2019",

    // Features section
    "features.label": "What it does",
    "features.title": "Everything you need, nothing you don't",
    "features.subtitle": "Powerful features built for Discord servers of all sizes.",
    "features.voice.title": "Voice Management",
    "features.voice.desc": "Create temporary voice channels with full control — lock, hide, permit, kick, transfer.",
    "features.xp.title": "XP & Leveling",
    "features.xp.desc": "Earn XP from messages, voice, and reactions. Track ranks, view canvas cards, and compete on leaderboards.",
    "features.economy.title": "Economy System",
    "features.economy.desc": "Coins, gems, daily prayers, streak rewards, and a server shop with purchasable roles and items.",
    "features.manga.title": "Manga Reader",
    "features.manga.desc": "Read from 6+ sources directly in Discord — nhentai, 3hentai, asmhentai, hentaifox & more.",
    "features.i18n.title": "Multi-Language",
    "features.i18n.desc": "Supports 15 languages — English, Vietnamese, Indonesian, Spanish, Japanese, Chinese, Korean, Portuguese (Brazil), French, German, Russian, Turkish, Italian, Polish, and Dutch.",
    "features.utility.title": "Utility Tools",
    "features.utility.desc": "Weather, translation, avatar viewer, bot info — plus 100% slash commands with auto-complete.",

    // Commands showcase section
    "showcase.label": "Commands",
    "showcase.title": "See it in action",
    "showcase.subtitle": "Discord-native embed previews of real bot responses.",
    "showcase.cta": "View all commands →",

    // Voice demo section
    "voice.label": "Voice Management",
    "voice.title": "Your room, your rules",
    "voice.subtitle": "Temporary voice channels that create themselves and clean up after.",
    "voice.step1.title": "Join the trigger channel",
    "voice.step1.desc": "Any channel with a \"3AT \" prefix acts as a trigger. Just join it.",
    "voice.step2.title": "Bot auto-creates your room",
    "voice.step2.desc": "A private voice channel is instantly created just for you.",
    "voice.step3.title": "Use the control panel",
    "voice.step3.desc": "Lock, hide, permit, kick, rename — full control via slash commands.",
    "voice.step4.title": "Auto-cleanup when empty",
    "voice.step4.desc": "When everyone leaves, the channel is deleted automatically.",

    // Stats
    "stats.servers": "Servers",
    "stats.users": "Users",
    "stats.uptime": "Uptime",
    "stats.since": "Since",

    // Testimonials
    "testimonials.label": "Community",
    "testimonials.title": "What we hear from servers",
    "testimonials.subtitle": "Short notes in the spirit of real tickets and DMs — common themes, not paid quotes.",

    // FAQ section
    "faq.label": "FAQ",
    "faq.title": "Common questions",
    "faq.subtitle": "Quick answers to the questions we hear most often.",
    "faq.xp.q": "How does the XP and leveling system work?",
    "faq.xp.a": "Members earn XP from messages, voice activity, and reactions. XP is tracked per server with configurable rates. Use /rank to view your level card, /leaderboard for rankings (with daily, weekly, monthly filters), and /server-rank to see your server's stats. Admins can configure XP rates and blacklist channels via /xp commands.",
    "faq.voice.q": "How do I set up temporary voice channels?",
    "faq.voice.a": "Create a voice channel with \"3AT \" prefix (e.g., \"3AT Create Room\"). When users join it, the bot automatically creates a personal channel for them. No additional configuration needed.",
    "faq.economy.q": "What is the economy system?",
    "faq.economy.a": "Each server has its own economy with coins and gems. Use /pray daily to earn coins (with streak bonuses at 3, 7, 14, and 30 days). Check your balance with /balance, and browse the server shop with /shop. Admins can manage currency with /economy commands.",
    "faq.lang.q": "What languages are supported?",
    "faq.lang.a": "The bot supports 15 languages: English, Vietnamese, Indonesian, Spanish, Japanese, Chinese, Korean, Portuguese (Brazil), French, German, Russian, Turkish, Italian, Polish, and Dutch. Set your personal language with /settings language, or set a server default with /settings server-language. The bot auto-detects your Discord client language as a fallback.",
    "faq.nsfw.q": "Is the manga reader NSFW only?",
    "faq.nsfw.a": "Yes. All manga commands require an NSFW-enabled channel. The bot checks the channel setting before responding and will show an error if the channel is not marked as NSFW.",
    "faq.perms.q": "What permissions does the bot need?",
    "faq.perms.a": "Administrator permission is recommended for full functionality. At minimum, the bot needs: Manage Channels (voice management), Send Messages, Embed Links, Attach Files (rank cards), and Connect + Move Members (voice features).",
    "faq.bug.q": "How do I report a bug or request a feature?",
    "faq.bug.a": "Open an issue on our GitHub repository or start a discussion in GitHub Discussions. You can also reach us through the Support server link in the navbar.",

    // Footer
    "footer.desc": "Discord bot for voice management, manga reading & more. Running since 2019.",
    "footer.links": "Links",
    "footer.resources": "Resources",
    "footer.legal": "Legal",
    "footer.add": "Add to Server",
    "footer.commands": "Commands",
    "footer.support": "Support Server",
    "footer.github": "GitHub",
    "footer.docs": "Documentation",
    "footer.bug": "Report Bug",
    "footer.discussions": "Discussions",
    "footer.privacy": "Privacy Policy",
    "footer.terms": "Terms of Service",

    // Commands page
    "commands.title": "All Commands",
    "commands.available": "commands available",
    "commands.search": "Search commands...",
    "commands.back": "← Back to Commands",
    "commands.guide": "Guide →",
    "commands.categories": "Categories",
    "commands.search.label": "Search",

    // Guide page
    "guide.section": "User Guide",
    "guide.title": "Everything you need to know about 3AT",
    "guide.subtitle": "Guides for every system — whether you're a member looking to earn coins or an admin setting up your server.",
    "guide.back": "← Back to Guide",
    "guide.related": "Related Commands",
    "guide.related.guides": "Related Guides",
    "guide.read": "Read guide →",

    // Shared
    "translation.notice": "Translation is being updated. Content is currently displayed in English.",
  },
  vi: {
    // Meta
    "meta.description": "Bot Discord quản lý kênh voice, đọc manga, dịch thuật và nhiều tính năng khác.",

    // Navigation
    "nav.features": "Tính năng",
    "nav.commands": "Lệnh",
    "nav.guide": "Hướng dẫn",
    "nav.faq": "Câu hỏi",
    "nav.support": "Hỗ trợ",
    "nav.add": "Thêm vào Server",

    // Hero
    "hero.subtitle": "Bot Discord với hệ thống XP, kinh tế, quản lý voice, đọc manga, hỗ trợ 15 ngôn ngữ. Chỉ dùng slash commands, luôn hoạt động.",
    "hero.add": "Thêm vào Server",
    "hero.commands": "Xem lệnh",
    "hero.trust": "Miễn phí · Slash commands · Hoạt động từ 2019",

    // Features section
    "features.label": "Tính năng",
    "features.title": "Tất cả những gì bạn cần, không gì thừa",
    "features.subtitle": "Các tính năng mạnh mẽ cho mọi server Discord.",
    "features.voice.title": "Quản lý Voice",
    "features.voice.desc": "Tạo kênh voice tạm thời với đầy đủ quyền — khóa, ẩn, cấp phép, kick, chuyển quyền.",
    "features.xp.title": "XP & Cấp độ",
    "features.xp.desc": "Nhận XP từ tin nhắn, voice và reaction. Xem rank card, bảng xếp hạng theo ngày/tuần/tháng.",
    "features.economy.title": "Hệ thống Kinh tế",
    "features.economy.desc": "Coin, gem, cầu nguyện hàng ngày, phần thưởng streak, và cửa hàng server với role và vật phẩm.",
    "features.manga.title": "Đọc Manga",
    "features.manga.desc": "Đọc từ 6+ nguồn trực tiếp trên Discord — nhentai, 3hentai, asmhentai, hentaifox & nhiều hơn.",
    "features.i18n.title": "Đa ngôn ngữ",
    "features.i18n.desc": "Hỗ trợ 15 ngôn ngữ — Tiếng Anh, Tiếng Việt, Indonesia, Tây Ban Nha, Nhật, Trung, Hàn, Bồ Đào Nha (Brazil), Pháp, Đức, Nga, Thổ Nhĩ Kỳ, Ý, Ba Lan và Hà Lan.",
    "features.utility.title": "Tiện ích",
    "features.utility.desc": "Thời tiết, dịch thuật, xem avatar, thông tin bot — cùng 100% slash commands với auto-complete.",

    // Commands showcase section
    "showcase.label": "Lệnh",
    "showcase.title": "Xem thực tế",
    "showcase.subtitle": "Xem trước embed Discord của các phản hồi từ bot.",
    "showcase.cta": "Xem tất cả lệnh →",

    // Voice demo section
    "voice.label": "Quản lý Voice",
    "voice.title": "Phòng của bạn, luật của bạn",
    "voice.subtitle": "Kênh voice tạm thời tự tạo và tự dọn dẹp.",
    "voice.step1.title": "Tham gia kênh trigger",
    "voice.step1.desc": "Bất kỳ kênh nào có tiền tố \"3AT \" đều là trigger. Chỉ cần tham gia.",
    "voice.step2.title": "Bot tự tạo phòng cho bạn",
    "voice.step2.desc": "Kênh voice riêng được tạo ngay lập tức cho bạn.",
    "voice.step3.title": "Sử dụng bảng điều khiển",
    "voice.step3.desc": "Khóa, ẩn, cấp phép, kick, đổi tên — toàn quyền qua slash commands.",
    "voice.step4.title": "Tự dọn dẹp khi trống",
    "voice.step4.desc": "Khi mọi người rời đi, kênh sẽ tự động xóa.",

    // Stats
    "stats.servers": "Servers",
    "stats.users": "Người dùng",
    "stats.uptime": "Uptime",
    "stats.since": "Từ năm",

    // Testimonials
    "testimonials.label": "Cộng đồng",
    "testimonials.title": "Phản hồi từ các server",
    "testimonials.subtitle": "Ghi chú ngắn theo tinh thần của các ticket và DM thực tế — chủ đề phổ biến, không phải đánh giá trả phí.",

    // FAQ section
    "faq.label": "Câu hỏi",
    "faq.title": "Câu hỏi thường gặp",
    "faq.subtitle": "Câu trả lời nhanh cho các câu hỏi phổ biến nhất.",
    "faq.xp.q": "Hệ thống XP và cấp độ hoạt động như thế nào?",
    "faq.xp.a": "Thành viên nhận XP từ tin nhắn, hoạt động voice và reaction. XP được tính theo từng server với tỉ lệ có thể tùy chỉnh. Dùng /rank để xem rank card, /leaderboard để xem bảng xếp hạng (theo ngày, tuần, tháng), và /server-rank để xem thống kê server. Admin có thể cấu hình tỉ lệ XP qua lệnh /xp.",
    "faq.voice.q": "Làm sao để thiết lập kênh voice tạm thời?",
    "faq.voice.a": "Tạo kênh voice có tiền tố \"3AT \" (ví dụ: \"3AT Create Room\"). Khi người dùng tham gia, bot tự động tạo kênh riêng cho họ. Không cần cấu hình thêm.",
    "faq.economy.q": "Hệ thống kinh tế là gì?",
    "faq.economy.a": "Mỗi server có kinh tế riêng với coin và gem. Dùng /pray mỗi ngày để nhận coin (có thưởng streak ở ngày 3, 7, 14, 30). Kiểm tra số dư với /balance, và xem cửa hàng với /shop. Admin quản lý tiền tệ qua lệnh /economy.",
    "faq.lang.q": "Hỗ trợ những ngôn ngữ nào?",
    "faq.lang.a": "Bot hỗ trợ 15 ngôn ngữ: Tiếng Anh, Tiếng Việt, Indonesia, Tây Ban Nha, Nhật, Trung, Hàn, Bồ Đào Nha (Brazil), Pháp, Đức, Nga, Thổ Nhĩ Kỳ, Ý, Ba Lan và Hà Lan. Đặt ngôn ngữ cá nhân với /settings language, hoặc mặc định server với /settings server-language.",
    "faq.nsfw.q": "Manga reader chỉ dùng cho NSFW?",
    "faq.nsfw.a": "Đúng. Tất cả lệnh manga yêu cầu kênh NSFW. Bot kiểm tra cài đặt kênh trước khi phản hồi và báo lỗi nếu kênh chưa được đánh dấu NSFW.",
    "faq.perms.q": "Bot cần những quyền gì?",
    "faq.perms.a": "Quyền Administrator được khuyến nghị. Tối thiểu cần: Manage Channels (quản lý voice), Send Messages, Embed Links, Attach Files (rank card), và Connect + Move Members (tính năng voice).",
    "faq.bug.q": "Làm sao báo lỗi hoặc yêu cầu tính năng?",
    "faq.bug.a": "Mở issue trên GitHub hoặc tạo thảo luận trong GitHub Discussions. Bạn cũng có thể liên hệ qua link Support trên thanh điều hướng.",

    // Footer
    "footer.desc": "Bot Discord quản lý voice, đọc manga & nhiều tính năng khác. Hoạt động từ 2019.",
    "footer.links": "Liên kết",
    "footer.resources": "Tài nguyên",
    "footer.legal": "Pháp lý",
    "footer.add": "Thêm vào Server",
    "footer.commands": "Lệnh",
    "footer.support": "Server Hỗ trợ",
    "footer.github": "GitHub",
    "footer.docs": "Tài liệu",
    "footer.bug": "Báo lỗi",
    "footer.discussions": "Thảo luận",
    "footer.privacy": "Chính sách Bảo mật",
    "footer.terms": "Điều khoản Dịch vụ",

    // Commands page
    "commands.title": "Tất cả Lệnh",
    "commands.available": "lệnh khả dụng",
    "commands.search": "Tìm lệnh...",
    "commands.back": "← Quay lại Lệnh",
    "commands.guide": "Hướng dẫn →",
    "commands.categories": "Danh mục",
    "commands.search.label": "Tìm kiếm",

    // Guide page
    "guide.section": "Hướng dẫn",
    "guide.title": "Mọi thứ bạn cần biết về 3AT",
    "guide.subtitle": "Hướng dẫn cho mọi hệ thống — dù bạn là thành viên muốn kiếm coin hay admin thiết lập server.",
    "guide.back": "← Quay lại Hướng dẫn",
    "guide.related": "Lệnh liên quan",
    "guide.related.guides": "Hướng dẫn liên quan",
    "guide.read": "Đọc hướng dẫn →",

    // Shared
    "translation.notice": "Bản dịch đang được cập nhật. Nội dung hiện tại hiển thị bằng tiếng Anh.",
  },
} as const;
```

- [ ] **Step 3: Create `src/i18n/utils.ts`**

```ts
import { ui, defaultLang, showDefaultLang } from "./ui";

export type Lang = keyof typeof ui;
export type TranslationKey = keyof (typeof ui)[typeof defaultLang];

export function getLangFromUrl(url: URL): Lang {
  const [, lang] = url.pathname.split("/");
  if (lang in ui) return lang as Lang;
  return defaultLang;
}

export function useTranslations(lang: Lang) {
  return function t(key: TranslationKey | (string & {})): string {
    return (ui[lang] as Record<string, string>)[key] || (ui[defaultLang] as Record<string, string>)[key] || key;
  };
}

export function useTranslatedPath(lang: Lang) {
  return function translatePath(path: string): string {
    return !showDefaultLang && lang === defaultLang ? path : `/${lang}${path}`;
  };
}
```

The `t()` function accepts both strict `TranslationKey` (with autocomplete) and arbitrary strings (for dynamic keys like `` `features.${key}.title` ``), falling back to the key itself if not found.

- [ ] **Step 4: Verify build passes**

Run: `cd landing && npm run build`
Expected: Build succeeds (new files are not imported yet, so no impact)

- [ ] **Step 5: Commit**

```bash
git add landing/astro.config.mjs landing/src/i18n/
git commit -m "feat(landing): add Astro i18n config and translation layer

Add i18n routing config (EN default, VI prefixed) and create
src/i18n/ui.ts + utils.ts following Astro Recipe pattern with
full EN/VI translations for all UI strings."
```

---

### Task 2: BaseLayout + LanguageSwitcher Refactoring

**Files:**
- Modify: `landing/src/layouts/BaseLayout.astro`
- Modify: `landing/src/components/LanguageSwitcher.astro`

- [ ] **Step 1: Refactor BaseLayout to accept lang prop and set `<html lang>` dynamically**

Replace the entire frontmatter and `<html>` tag in `landing/src/layouts/BaseLayout.astro`:

```astro
---
import Navbar from "../components/Navbar.astro";
import Footer from "../components/Footer.astro";
import BackToTop from "../components/BackToTop.astro";
import { getLangFromUrl, useTranslations } from "../i18n/utils";
import type { Lang } from "../i18n/utils";
import "../styles/global.css";
import "../styles/animations.css";

interface Props {
  title: string;
  description?: string;
  lang?: Lang;
  hreflang?: Array<{ lang: string; href: string }>;
}

const lang = Astro.props.lang ?? getLangFromUrl(Astro.url);
const t = useTranslations(lang);
const {
  title,
  description = t("meta.description"),
  hreflang,
} = Astro.props;
---

<!doctype html>
<html lang={lang}>
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <link rel="icon" type="image/gif" href="/maid.gif" />
    <link rel="preconnect" href="https://fonts.googleapis.com" />
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
    <link
      href="https://fonts.googleapis.com/css2?family=Outfit:wght@500;600;700&family=Source+Sans+3:wght@400;500;600;700&display=swap"
      rel="stylesheet"
    />
    <title>{title}</title>
    {hreflang?.map((alt) => (
      <link rel="alternate" hreflang={alt.lang} href={alt.href} />
    ))}
  </head>
  <body>
    <Navbar lang={lang} />
    <main style="padding-top: 56px;">
      <slot />
    </main>
    <Footer lang={lang} />
    <BackToTop />
    <script src="../scripts/scroll-animations.ts"></script>
    <script src="../scripts/counter.ts"></script>
  </body>
</html>
```

- [ ] **Step 2: Refactor LanguageSwitcher to work on all pages**

Replace the entire `landing/src/components/LanguageSwitcher.astro`:

```astro
---
import { languages, defaultLang } from "../i18n/ui";
import { getLangFromUrl } from "../i18n/utils";
import type { Lang } from "../i18n/utils";

interface Props {
  lang?: Lang;
}

const currentLang = Astro.props.lang ?? getLangFromUrl(Astro.url);
const pathname = Astro.url.pathname;

// Strip current locale prefix to get the base path
function getBasePath(path: string): string {
  for (const lang of Object.keys(languages)) {
    if (path === `/${lang}` || path === `/${lang}/`) return "/";
    if (path.startsWith(`/${lang}/`)) return path.slice(lang.length + 1);
  }
  return path;
}

const basePath = getBasePath(pathname);

function buildHref(lang: string): string {
  if (lang === defaultLang) return basePath;
  return `/${lang}${basePath === "/" ? "" : basePath}`;
}
---

<div class="lang-switcher">
  {Object.entries(languages).map(([code, label]) => (
    <a
      href={buildHref(code)}
      class:list={["lang-btn", { active: currentLang === code }]}
      aria-label={`Switch to ${label}`}
    >
      {code.toUpperCase()}
    </a>
  ))}
</div>

<style>
  .lang-switcher {
    display: flex;
    gap: 4px;
    align-items: center;
  }

  .lang-btn {
    color: var(--text-secondary);
    font-size: 12px;
    font-weight: 600;
    padding: 4px 8px;
    border-radius: var(--radius-sm);
    transition: color 0.2s, background 0.2s;
    text-decoration: none;
  }

  .lang-btn:hover {
    color: var(--text-primary);
    background: var(--accent-soft);
  }

  .lang-btn.active {
    color: var(--accent);
    background: rgba(88, 101, 242, 0.15);
  }
</style>
```

- [ ] **Step 3: Verify build passes**

Run: `cd landing && npm run build`
Expected: Build succeeds. Note: Navbar and Footer don't accept `lang` prop yet — Astro ignores unknown props on components, so no error.

- [ ] **Step 4: Commit**

```bash
git add landing/src/layouts/BaseLayout.astro landing/src/components/LanguageSwitcher.astro
git commit -m "feat(landing): refactor BaseLayout and LanguageSwitcher for i18n

BaseLayout now sets <html lang> dynamically and passes lang to
Navbar/Footer. LanguageSwitcher works on all pages by stripping
locale prefix and rebuilding paths."
```

---

### Task 3: Navbar + Footer i18n

**Files:**
- Modify: `landing/src/components/Navbar.astro`
- Modify: `landing/src/components/Footer.astro`

- [ ] **Step 1: Refactor Navbar.astro**

Replace the entire frontmatter section (lines 1–13) of `landing/src/components/Navbar.astro`:

```astro
---
import LanguageSwitcher from "./LanguageSwitcher.astro";
import { useTranslations, useTranslatedPath } from "../i18n/utils";
import type { Lang } from "../i18n/utils";

interface Props {
  lang?: Lang;
}

const { lang = "en" } = Astro.props;
const t = useTranslations(lang);
const tp = useTranslatedPath(lang);

const navLinks = [
  { href: tp("/#features"), label: t("nav.features") },
  { href: tp("/commands"), label: t("nav.commands") },
  { href: tp("/guide"), label: t("nav.guide") },
  { href: tp("/#faq"), label: t("nav.faq") },
  {
    href: "https://github.com/3Tea/discord-bot/discussions",
    label: t("nav.support"),
    external: true,
  },
];

const inviteUrl =
  "https://discord.com/api/oauth2/authorize?client_id=615052741911379983&permissions=8&scope=applications.commands%20bot";
---
```

Then in the template, replace the "Add to Server" button text and add LanguageSwitcher after it. Replace the `<div class="navbar-menu">` section:

```astro
    <div class="navbar-menu">
      <ul class="navbar-links">
        {navLinks.map((link) => (
          <li>
            <a
              href={link.href}
              class="navbar-link"
              {...(link.external ? { target: "_blank", rel: "noopener" } : {})}
            >
              {link.label}
            </a>
          </li>
        ))}
      </ul>
      <LanguageSwitcher lang={lang} />
      <a href={inviteUrl} target="_blank" rel="noopener" class="btn btn-primary btn-sm">
        {t("nav.add")}
      </a>
    </div>
```

All `<style>` and `<script>` sections remain unchanged.

- [ ] **Step 2: Refactor Footer.astro**

Replace the entire frontmatter section (lines 1–43) of `landing/src/components/Footer.astro`:

```astro
---
import { useTranslations, useTranslatedPath } from "../i18n/utils";
import type { Lang } from "../i18n/utils";

interface Props {
  lang?: Lang;
}

const { lang = "en" } = Astro.props;
const t = useTranslations(lang);
const tp = useTranslatedPath(lang);

const inviteUrl =
  "https://discord.com/api/oauth2/authorize?client_id=615052741911379983&permissions=8&scope=applications.commands%20bot";

const links = [
  { label: t("footer.add"), href: inviteUrl, external: true },
  { label: t("footer.commands"), href: tp("/commands") },
  {
    label: t("footer.support"),
    href: "https://github.com/3Tea/discord-bot/discussions",
    external: true,
  },
  {
    label: t("footer.github"),
    href: "https://github.com/3Tea/discord-bot",
    external: true,
  },
];

const resources = [
  {
    label: t("footer.docs"),
    href: "https://github.com/3Tea/discord-bot#readme",
    external: true,
  },
  {
    label: t("footer.bug"),
    href: "https://github.com/3Tea/discord-bot/issues",
    external: true,
  },
  {
    label: t("footer.discussions"),
    href: "https://github.com/3Tea/discord-bot/discussions",
    external: true,
  },
];

const legal = [
  { label: t("footer.privacy"), href: "/privacy" },
  { label: t("footer.terms"), href: "/terms" },
];

const year = new Date().getFullYear();
---
```

In the template, replace the hardcoded heading strings:

- `<h4 class="footer-heading">Links</h4>` → `<h4 class="footer-heading">{t("footer.links")}</h4>`
- `<h4 class="footer-heading">Resources</h4>` → `<h4 class="footer-heading">{t("footer.resources")}</h4>`
- `<h4 class="footer-heading">Legal</h4>` → `<h4 class="footer-heading">{t("footer.legal")}</h4>`
- The `<p class="footer-desc">` text → `<p class="footer-desc">{t("footer.desc")}</p>`

All `<style>` remains unchanged.

- [ ] **Step 3: Verify build passes**

Run: `cd landing && npm run build`
Expected: Build succeeds

- [ ] **Step 4: Commit**

```bash
git add landing/src/components/Navbar.astro landing/src/components/Footer.astro
git commit -m "feat(landing): add i18n to Navbar and Footer

Navbar and Footer now accept lang prop, use t() for all text
and useTranslatedPath() for internal links. LanguageSwitcher
added to Navbar for site-wide language switching."
```

---

### Task 4: Homepage Section Components (Hero, Features, FAQ)

**Files:**
- Modify: `landing/src/components/Hero.astro`
- Modify: `landing/src/components/Features.astro`
- Modify: `landing/src/components/FAQ.astro`
- Modify: `landing/src/data/features.ts`
- Delete: `landing/src/data/faq.ts`

- [ ] **Step 1: Refactor Hero.astro**

Replace the frontmatter and all template text in `landing/src/components/Hero.astro`. Replace lines 1–4:

```astro
---
import { useTranslations, useTranslatedPath } from "../i18n/utils";
import type { Lang } from "../i18n/utils";

interface Props {
  lang?: Lang;
}

const { lang = "en" } = Astro.props;
const t = useTranslations(lang);
const tp = useTranslatedPath(lang);

const inviteUrl =
  "https://discord.com/api/oauth2/authorize?client_id=615052741911379983&permissions=8&scope=applications.commands%20bot";
---
```

In the template, replace the hardcoded strings:

- `<p class="hero-subtitle">A Discord bot packed with...</p>` → `<p class="hero-subtitle">{t("hero.subtitle")}</p>`
- `Add to Server` button text (inside the `<a>` with Discord SVG) → `{t("hero.add")}`
- `<a href="#commands" class="btn btn-secondary">View Commands</a>` → `<a href={tp("/#commands")} class="btn btn-secondary">{t("hero.commands")}</a>`
- `<p class="hero-trust">Free to use &middot; ...</p>` → `<p class="hero-trust">{t("hero.trust")}</p>`

All `<style>` remains unchanged.

- [ ] **Step 2: Simplify features.ts — remove text, keep icons only**

Replace the entire `landing/src/data/features.ts`:

```ts
export const featureIcons = [
  "🎙️",  // voice
  "📊",  // xp
  "💰",  // economy
  "📖",  // manga
  "🌐",  // i18n
  "🌤️",  // utility
] as const;

export const featureKeys = [
  "voice",
  "xp",
  "economy",
  "manga",
  "i18n",
  "utility",
] as const;
```

- [ ] **Step 3: Refactor Features.astro to use t()**

Replace the entire frontmatter and template of `landing/src/components/Features.astro` (keep all `<style>` and `<script>` as-is):

```astro
---
import { featureIcons, featureKeys } from "../data/features";
import { useTranslations } from "../i18n/utils";
import type { Lang } from "../i18n/utils";

interface Props {
  lang?: Lang;
}

const { lang = "en" } = Astro.props;
const t = useTranslations(lang);
---

<section id="features" class="features-section">
  <div class="container">
    <div class="section-header">
      <p class="section-label">{t("features.label")}</p>
      <h2 class="section-title">{t("features.title")}</h2>
      <p class="section-subtitle">{t("features.subtitle")}</p>
    </div>
    <div class="features-grid reveal-stagger">
      {featureKeys.map((key, i) => (
        <div class="feature-card reveal" style={`--i: ${i}`}>
          <div class="feature-icon">{featureIcons[i]}</div>
          <h3 class="feature-title">{t(`features.${key}.title`)}</h3>
          <p class="feature-desc">{t(`features.${key}.desc`)}</p>
        </div>
      ))}
    </div>
  </div>
</section>
```

Note: Dynamic keys like `` `features.${key}.title` `` work because `useTranslations` (defined in Task 1) accepts both strict `TranslationKey` and arbitrary `string` types.

- [ ] **Step 4: Delete faq.ts and refactor FAQ.astro**

Delete `landing/src/data/faq.ts`.

Replace the frontmatter and template of `landing/src/components/FAQ.astro` (keep all `<style>` as-is):

```astro
---
import { useTranslations } from "../i18n/utils";
import type { Lang } from "../i18n/utils";

interface Props {
  lang?: Lang;
}

const { lang = "en" } = Astro.props;
const t = useTranslations(lang);

const faqKeys = ["xp", "voice", "economy", "lang", "nsfw", "perms", "bug"] as const;
---

<section id="faq" class="faq-section">
  <div class="container">
    <div class="section-header">
      <p class="section-label">{t("faq.label")}</p>
      <h2 class="section-title">{t("faq.title")}</h2>
      <p class="section-subtitle">{t("faq.subtitle")}</p>
    </div>

    <div class="faq-list reveal-stagger">
      {faqKeys.map((key, i) => (
        <details class="faq-item reveal" style={`--i: ${i}`} open={i === 0}>
          <summary class="faq-summary">
            <span class="faq-question">{t(`faq.${key}.q`)}</span>
            <svg
              class="faq-chevron"
              width="18"
              height="18"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              aria-hidden="true"
            >
              <polyline points="6 9 12 15 18 9" />
            </svg>
          </summary>
          <div class="faq-content">
            <p>{t(`faq.${key}.a`)}</p>
          </div>
        </details>
      ))}
    </div>
  </div>
</section>
```

- [ ] **Step 5: Verify build passes**

Run: `cd landing && npm run build`
Expected: Build succeeds

- [ ] **Step 6: Commit**

```bash
git add landing/src/components/Hero.astro landing/src/components/Features.astro landing/src/components/FAQ.astro landing/src/data/features.ts
git rm landing/src/data/faq.ts
git commit -m "feat(landing): add i18n to Hero, Features, FAQ components

All three components now accept lang prop and use t() for text.
features.ts simplified to icons/keys only, faq.ts deleted — all
text now lives in i18n/ui.ts translations."
```

---

### Task 5: Homepage Section Components (CommandsShowcase, VoiceDemo, Stats, Testimonials)

**Files:**
- Modify: `landing/src/components/CommandsShowcase.astro`
- Modify: `landing/src/components/VoiceDemo.astro`
- Modify: `landing/src/components/Stats.astro`
- Modify: `landing/src/components/Testimonials.astro`

- [ ] **Step 1: Refactor CommandsShowcase.astro**

Add frontmatter and replace section header + CTA link. Add at the top:

```astro
---
import { useTranslations, useTranslatedPath } from "../i18n/utils";
import type { Lang } from "../i18n/utils";

interface Props {
  lang?: Lang;
}

const { lang = "en" } = Astro.props;
const t = useTranslations(lang);
const tp = useTranslatedPath(lang);
---
```

In the template, replace:

- `<p class="section-label">Commands</p>` → `<p class="section-label">{t("showcase.label")}</p>`
- `<h2 class="section-title">See it in action</h2>` → `<h2 class="section-title">{t("showcase.title")}</h2>`
- `<p class="section-subtitle">Discord-native embed previews of real bot responses.</p>` → `<p class="section-subtitle">{t("showcase.subtitle")}</p>`
- `<a href="/commands" class="cta-link">View all commands &rarr;</a>` → `<a href={tp("/commands")} class="cta-link">{t("showcase.cta")}</a>`

The Discord embed mockup content (Channel Locked, Random Doujin, Rank Card, field names/values) stays in English — these represent actual Discord bot responses.

- [ ] **Step 2: Refactor VoiceDemo.astro**

Replace the frontmatter (lines 1–34):

```astro
---
import { useTranslations } from "../i18n/utils";
import type { Lang } from "../i18n/utils";

interface Props {
  lang?: Lang;
}

const { lang = "en" } = Astro.props;
const t = useTranslations(lang);

const steps = [
  { number: 1, title: t("voice.step1.title"), desc: t("voice.step1.desc") },
  { number: 2, title: t("voice.step2.title"), desc: t("voice.step2.desc") },
  { number: 3, title: t("voice.step3.title"), desc: t("voice.step3.desc") },
  { number: 4, title: t("voice.step4.title"), desc: t("voice.step4.desc") },
];

const controlButtons = [
  { label: "Lock", active: true },
  { label: "Unlock", active: false },
  { label: "Hide", active: false },
  { label: "Show", active: false },
  { label: "Permit", active: false },
  { label: "Kick", active: false },
  { label: "Transfer", active: false },
  { label: "Rename", active: false },
];
---
```

In the template, replace section header strings:

- `<p class="section-label">Voice Management</p>` → `<p class="section-label">{t("voice.label")}</p>`
- `<h2 class="section-title">Your room, your rules</h2>` → `<h2 class="section-title">{t("voice.title")}</h2>`
- `<p class="section-subtitle">Temporary voice channels...</p>` → `<p class="section-subtitle">{t("voice.subtitle")}</p>`

The Discord sidebar mockup (server name, channel names, user names, control button labels) stays in English — these represent actual Discord UI.

- [ ] **Step 3: Refactor Stats.astro**

Replace the frontmatter (lines 1–8):

```astro
---
import { useTranslations } from "../i18n/utils";
import type { Lang } from "../i18n/utils";

interface Props {
  lang?: Lang;
}

const { lang = "en" } = Astro.props;
const t = useTranslations(lang);

const stats = [
  { value: "500+", label: t("stats.servers"), target: 500 },
  { value: "50K+", label: t("stats.users"), target: 50000 },
  { value: "99.9%", label: t("stats.uptime"), target: null },
  { value: "2019", label: t("stats.since"), target: null },
];
---
```

Template and styles remain unchanged.

- [ ] **Step 4: Refactor Testimonials.astro**

Replace the frontmatter (lines 1–29):

```astro
---
import { useTranslations } from "../i18n/utils";
import type { Lang } from "../i18n/utils";

interface Props {
  lang?: Lang;
}

const { lang = "en" } = Astro.props;
const t = useTranslations(lang);

/**
 * Community feedback — copy is written as typical admin/mod notes (composite themes),
 * not fabricated "perfect" reviews. Keeps the landing honest.
 */
const reviews = [
  {
    name: "vln",
    role: "Server owner · ~400 members",
    color: "#5865F2",
    quote:
      "We added it for XP, but the temp voice flow actually cut down how often people ping me to make a private room. Join the trigger channel, get a room, it deletes when empty.",
  },
  {
    name: "mira",
    role: "Moderator",
    color: "#ED4245",
    quote:
      "Honestly thought the manga stuff would be niche, but our NSFW channel uses it a lot. In-server beats dumping links in chat.",
  },
  {
    name: "hx",
    role: "Admin",
    color: "#3BA55C",
    quote:
      "Nothing flashy — leaderboard and /rank are what our members open daily. It's been running for years without drama, and translate helps when someone joins who doesn't speak English.",
  },
];
---
```

In the template, replace section header strings:

- `<p class="section-label">Community</p>` → `<p class="section-label">{t("testimonials.label")}</p>`
- `<h2 class="section-title">What we hear from servers</h2>` → `<h2 class="section-title">{t("testimonials.title")}</h2>`
- `<p class="section-subtitle">Short notes in the spirit...</p>` → `<p class="section-subtitle">{t("testimonials.subtitle")}</p>`

Testimonial quotes/roles stay in English — they are attributed quotes from specific users.

- [ ] **Step 5: Verify build passes**

Run: `cd landing && npm run build`
Expected: Build succeeds

- [ ] **Step 6: Commit**

```bash
git add landing/src/components/CommandsShowcase.astro landing/src/components/VoiceDemo.astro landing/src/components/Stats.astro landing/src/components/Testimonials.astro
git commit -m "feat(landing): add i18n to CommandsShowcase, VoiceDemo, Stats, Testimonials

Section headers and labels translated via t(). Discord mockup
content and testimonial quotes stay in English as they represent
actual bot responses and attributed user quotes."
```

---

### Task 6: Commands Page Components (CommandsSidebar, CommandCard)

**Files:**
- Modify: `landing/src/components/CommandsSidebar.astro`
- Modify: `landing/src/components/CommandCard.astro`

- [ ] **Step 1: Refactor CommandsSidebar.astro**

Replace the entire frontmatter (lines 1–12):

```astro
---
import { useTranslations } from "../i18n/utils";
import type { Lang } from "../i18n/utils";

interface Props {
  lang?: Lang;
}

const { lang = "en" } = Astro.props;
const t = useTranslations(lang);

const categories = [
  { key: "all", label: t("commands.title") },
  { key: "voice", label: "🎙️ Voice" },
  { key: "xp", label: "📊 XP & Leveling" },
  { key: "economy", label: "💰 Economy" },
  { key: "moderation", label: "🛡️ Moderation" },
  { key: "manga", label: "📖 Manga" },
  { key: "utility", label: "🛠️ Utility" },
  { key: "info", label: "ℹ️ Info" },
  { key: "settings", label: "⚙️ Settings" },
];
---
```

In the template, replace:

- `<p class="sidebar-label">Categories</p>` → `<p class="sidebar-label">{t("commands.categories")}</p>`
- `<p class="sidebar-label">Search</p>` → `<p class="sidebar-label">{t("commands.search.label")}</p>`
- `placeholder="🔍 Search commands..."` → `placeholder={`🔍 ${t("commands.search")}`}`

Note: Category labels (Voice, XP & Leveling, etc.) are product names and stay in English. Only the "All Commands" first entry and section labels are translated.

- [ ] **Step 2: Refactor CommandCard.astro**

Add i18n import and replace the "Guide →" text. Replace the frontmatter:

```astro
---
import type { Command } from "../data/commands";
import { categoryMeta } from "../data/commands";
import { useTranslations } from "../i18n/utils";
import type { Lang } from "../i18n/utils";

interface Props {
  command: Command;
  href?: string;
  lang?: Lang;
}

const { command, href, lang = "en" } = Astro.props;
const t = useTranslations(lang);
const meta = categoryMeta[command.category];
const items = command.subcommands || command.options || [];
const maxShow = 4;
const overflow = items.length - maxShow;
---
```

In the template:

- Replace `<span class="guide-link-text">Guide →</span>` → `<span class="guide-link-text">{t("commands.guide")}</span>`
- Replace default href: `href={href || \`/en/commands/${command.name}\`}` → `href={href || \`/en/commands/${command.name}\`}` (href is always passed from the page component, so the default is just a safety fallback)

- [ ] **Step 3: Verify build passes**

Run: `cd landing && npm run build`
Expected: Build succeeds

- [ ] **Step 4: Commit**

```bash
git add landing/src/components/CommandsSidebar.astro landing/src/components/CommandCard.astro
git commit -m "feat(landing): add i18n to CommandsSidebar and CommandCard

Sidebar labels and search placeholder translated. CommandCard
'Guide' link text translated. Category names stay in English
as product terminology."
```

---

### Task 7: Guide Page Components (GuideCard, RelatedCommands, RelatedGuides)

**Files:**
- Modify: `landing/src/components/GuideCard.astro`
- Modify: `landing/src/components/RelatedCommands.astro`
- Modify: `landing/src/components/RelatedGuides.astro`

- [ ] **Step 1: Refactor GuideCard.astro**

Add i18n import. Replace the frontmatter:

```astro
---
import { useTranslations } from "../i18n/utils";
import type { Lang } from "../i18n/utils";

interface Props {
  title: string;
  description: string;
  icon: string;
  href: string;
  color: string;
  bg: string;
  lang?: Lang;
}

const { title, description, icon, href, color, bg, lang = "en" } = Astro.props;
const t = useTranslations(lang);
---
```

In the template, replace:

- `<span class="guide-card-link">Read guide →</span>` → `<span class="guide-card-link">{t("guide.read")}</span>`

- [ ] **Step 2: Refactor RelatedCommands.astro**

Replace the frontmatter:

```astro
---
import { commands, categoryMeta } from "../data/commands";
import type { Category } from "../data/commands";
import { useTranslations } from "../i18n/utils";
import type { Lang } from "../i18n/utils";

interface Props {
  currentCommand: string;
  category: string;
  lang: string;
}

const { currentCommand, category, lang } = Astro.props;
const t = useTranslations(lang as Lang);

const related = commands
  .filter((cmd) => cmd.category === category && cmd.name !== currentCommand)
  .slice(0, 3);
---
```

In the template, replace:

- `<h2 class="related-title">Related Commands</h2>` → `<h2 class="related-title">{t("guide.related")}</h2>`

- [ ] **Step 3: Refactor RelatedGuides.astro**

Replace the frontmatter:

```astro
---
import { guideMeta } from "../data/guides";
import { useTranslations } from "../i18n/utils";
import type { Lang } from "../i18n/utils";

interface Props {
  currentSlug: string;
  lang: string;
}

const { currentSlug, lang } = Astro.props;
const t = useTranslations(lang as Lang);

const allGuides = Object.values(guideMeta).filter((g) => g.slug !== currentSlug).slice(0, 2);
---
```

In the template, replace:

- `<h2 class="related-title">Related Guides</h2>` → `<h2 class="related-title">{t("guide.related.guides")}</h2>`

- [ ] **Step 4: Verify build passes**

Run: `cd landing && npm run build`
Expected: Build succeeds

- [ ] **Step 5: Commit**

```bash
git add landing/src/components/GuideCard.astro landing/src/components/RelatedCommands.astro landing/src/components/RelatedGuides.astro
git commit -m "feat(landing): add i18n to GuideCard, RelatedCommands, RelatedGuides

Translated 'Read guide', 'Related Commands', and 'Related Guides'
headings via t()."
```

---

### Task 8: Shared Page Components

**Files:**
- Create: `landing/src/components/pages/HomePage.astro`
- Create: `landing/src/components/pages/CommandsPage.astro`
- Create: `landing/src/components/pages/GuidePage.astro`

- [ ] **Step 1: Create HomePage.astro**

```astro
---
import BaseLayout from "../../layouts/BaseLayout.astro";
import Hero from "../Hero.astro";
import Features from "../Features.astro";
import CommandsShowcase from "../CommandsShowcase.astro";
import VoiceDemo from "../VoiceDemo.astro";
import Stats from "../Stats.astro";
import Testimonials from "../Testimonials.astro";
import FAQ from "../FAQ.astro";
import type { Lang } from "../../i18n/utils";

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;

const hreflang = [
  { lang: "en", href: "/" },
  { lang: "vi", href: "/vi" },
];
---

<BaseLayout title="3AT - Endless Paradox | Discord Bot" lang={lang} hreflang={hreflang}>
  <Hero lang={lang} />
  <Features lang={lang} />
  <CommandsShowcase lang={lang} />
  <VoiceDemo lang={lang} />
  <Stats lang={lang} />
  <Testimonials lang={lang} />
  <FAQ lang={lang} />
</BaseLayout>
```

- [ ] **Step 2: Create CommandsPage.astro**

```astro
---
import BaseLayout from "../../layouts/BaseLayout.astro";
import CommandsSidebar from "../CommandsSidebar.astro";
import CommandCard from "../CommandCard.astro";
import { commands } from "../../data/commands";
import { useTranslations, useTranslatedPath } from "../../i18n/utils";
import type { Lang } from "../../i18n/utils";

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);

const hreflang = [
  { lang: "en", href: "/commands" },
  { lang: "vi", href: "/vi/commands" },
];
---

<BaseLayout title={`${t("commands.title")} | 3AT - Endless Paradox`} lang={lang} hreflang={hreflang}>
  <div class="commands-page">
    <CommandsSidebar lang={lang} />

    <div class="commands-main">
      <div class="commands-header">
        <h1 class="commands-title">{t("commands.title")}</h1>
        <p class="commands-count">
          <span id="commands-count">{commands.length}</span> {t("commands.available")}
        </p>
      </div>

      <div class="commands-grid" id="commands-grid">
        {commands.map((cmd) => (
          <div data-category={cmd.category} data-name={cmd.name} data-desc={cmd.description}>
            <CommandCard command={cmd} href={`/${lang}/commands/${cmd.name}`} lang={lang} />
          </div>
        ))}
      </div>
    </div>
  </div>
</BaseLayout>

<style>
  .commands-page {
    display: flex;
    min-height: calc(100vh - 56px);
  }

  .commands-main {
    flex: 1;
    padding: 24px;
  }

  .commands-header {
    margin-bottom: 28px;
    padding-bottom: 16px;
    border-bottom: 1px solid var(--border);
  }

  .commands-title {
    font-family: var(--font-display);
    color: var(--text-primary);
    font-size: clamp(1.35rem, 3vw, 1.65rem);
    font-weight: 700;
    margin-bottom: 4px;
    letter-spacing: -0.02em;
  }

  .commands-count {
    color: var(--text-muted);
    font-size: 13px;
  }

  .commands-grid {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 12px;
  }

  @media (max-width: 768px) {
    .commands-page {
      flex-direction: column;
    }

    .commands-grid {
      grid-template-columns: 1fr;
    }
  }
</style>

<script>
  const sidebar = document.getElementById("commands-sidebar");
  const grid = document.getElementById("commands-grid");
  const searchInput = document.getElementById("command-search") as HTMLInputElement | null;
  const countEl = document.getElementById("commands-count");
  const titleEl = document.querySelector(".commands-title");
  const cards = grid ? Array.from(grid.children) as HTMLElement[] : [];

  let activeCategory = "all";

  function filterCards(): void {
    const query = searchInput?.value.toLowerCase() || "";
    let visible = 0;

    cards.forEach((card) => {
      const cat = card.dataset.category || "";
      const name = card.dataset.name || "";
      const desc = card.dataset.desc || "";

      const matchCategory = activeCategory === "all" || cat === activeCategory;
      const matchSearch =
        !query || name.includes(query) || desc.toLowerCase().includes(query);

      const show = matchCategory && matchSearch;
      card.style.display = show ? "" : "none";
      if (show) visible++;
    });

    if (countEl) countEl.textContent = String(visible);
  }

  sidebar?.querySelectorAll("[data-category]").forEach((btn) => {
    btn.addEventListener("click", () => {
      sidebar.querySelectorAll("[data-category]").forEach((b) => b.classList.remove("active"));
      btn.classList.add("active");
      activeCategory = (btn as HTMLElement).dataset.category || "all";

      const label =
        activeCategory === "all"
          ? btn.textContent?.trim() || "Commands"
          : btn.textContent?.trim() || "Commands";
      if (titleEl) titleEl.textContent = label;

      filterCards();
    });
  });

  let debounceTimer: ReturnType<typeof setTimeout>;
  searchInput?.addEventListener("input", () => {
    clearTimeout(debounceTimer);
    debounceTimer = setTimeout(filterCards, 200);
  });
</script>
```

- [ ] **Step 3: Create GuidePage.astro**

```astro
---
import BaseLayout from "../../layouts/BaseLayout.astro";
import GuideCard from "../GuideCard.astro";
import { getCollection } from "astro:content";
import { guideMeta } from "../../data/guides";
import { useTranslations } from "../../i18n/utils";
import type { Lang } from "../../i18n/utils";

interface Props {
  lang: Lang;
}

const { lang } = Astro.props;
const t = useTranslations(lang);

const allGuides = await getCollection("guides");
const langGuides = allGuides
  .filter((g) => g.id.startsWith(`${lang}/`))
  .sort((a, b) => a.data.order - b.data.order);

// Fallback to EN if no guides exist for this locale
const guides = langGuides.length > 0
  ? langGuides
  : allGuides.filter((g) => g.id.startsWith("en/")).sort((a, b) => a.data.order - b.data.order);

const hreflang = [
  { lang: "en", href: "/guide" },
  { lang: "vi", href: "/vi/guide" },
];
---

<BaseLayout
  title={`${t("guide.section")} | 3AT - Endless Paradox`}
  description={t("guide.subtitle")}
  lang={lang}
  hreflang={hreflang}
>
  <div class="container guide-index">
    <div class="guide-index-hero">
      <p class="section-label">{t("guide.section")}</p>
      <h1 class="section-title">{t("guide.title")}</h1>
      <p class="section-subtitle">{t("guide.subtitle")}</p>
    </div>

    <div class="guide-grid">
      {guides.map((guide) => {
        const guideSlug = guide.id.split("/").pop()!;
        const meta = guideMeta[guideSlug];
        return (
          <GuideCard
            title={guide.data.title}
            description={guide.data.description}
            icon={guide.data.icon}
            href={`/${lang}/guide/${guideSlug}`}
            color={meta?.color ?? "#5865F2"}
            bg={meta?.bg ?? "rgba(88,101,242,0.15)"}
            lang={lang}
          />
        );
      })}
    </div>
  </div>
</BaseLayout>

<style>
  .guide-index {
    padding-top: 32px;
    padding-bottom: 64px;
  }

  .guide-index-hero {
    text-align: center;
    margin-bottom: 40px;
  }

  .guide-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 16px;
  }

  @media (max-width: 900px) {
    .guide-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 560px) {
    .guide-grid {
      grid-template-columns: 1fr;
    }
  }
</style>
```

- [ ] **Step 4: Verify build passes**

Run: `cd landing && npm run build`
Expected: Build succeeds

- [ ] **Step 5: Commit**

```bash
git add landing/src/components/pages/
git commit -m "feat(landing): create shared page components for i18n

HomePage, CommandsPage, GuidePage accept lang prop and pass it
to all child components. These shared components eliminate logic
duplication between locale page files."
```

---

### Task 9: Page Files — Thin Wrappers + Vietnamese Pages

**Files:**
- Modify: `landing/src/pages/index.astro`
- Modify: `landing/src/pages/commands.astro`
- Modify: `landing/src/pages/guide.astro`
- Create: `landing/src/pages/vi/index.astro`
- Create: `landing/src/pages/vi/commands.astro`
- Create: `landing/src/pages/vi/guide.astro`

- [ ] **Step 1: Simplify root index.astro**

Replace the entire `landing/src/pages/index.astro`:

```astro
---
import HomePage from "../components/pages/HomePage.astro";
---

<HomePage lang="en" />
```

- [ ] **Step 2: Simplify root commands.astro**

Replace the entire `landing/src/pages/commands.astro`:

```astro
---
import CommandsPage from "../components/pages/CommandsPage.astro";
---

<CommandsPage lang="en" />
```

- [ ] **Step 3: Simplify root guide.astro**

Replace the entire `landing/src/pages/guide.astro`:

```astro
---
import GuidePage from "../components/pages/GuidePage.astro";
---

<GuidePage lang="en" />
```

- [ ] **Step 4: Create Vietnamese pages**

Create `landing/src/pages/vi/index.astro`:

```astro
---
import HomePage from "../../components/pages/HomePage.astro";
---

<HomePage lang="vi" />
```

Create `landing/src/pages/vi/commands.astro`:

```astro
---
import CommandsPage from "../../components/pages/CommandsPage.astro";
---

<CommandsPage lang="vi" />
```

Create `landing/src/pages/vi/guide.astro`:

```astro
---
import GuidePage from "../../components/pages/GuidePage.astro";
---

<GuidePage lang="vi" />
```

- [ ] **Step 5: Verify build passes**

Run: `cd landing && npm run build`
Expected: Build succeeds. New routes `/vi/`, `/vi/commands`, `/vi/guide` are generated.

- [ ] **Step 6: Commit**

```bash
git add landing/src/pages/
git commit -m "feat(landing): create thin page wrappers and Vietnamese pages

Root pages simplified to thin wrappers passing lang='en'.
New vi/ directory with index, commands, guide pages passing
lang='vi'. All pages delegate to shared page components."
```

---

### Task 10: Content Collection for Privacy/Terms

**Files:**
- Create: `landing/src/content/pages/en/privacy.md`
- Create: `landing/src/content/pages/en/terms.md`
- Modify: `landing/src/content.config.ts`
- Modify: `landing/src/pages/privacy.astro`
- Modify: `landing/src/pages/terms.astro`

- [ ] **Step 1: Create privacy.md**

Create `landing/src/content/pages/en/privacy.md` with the same HTML content currently in `privacy.astro` (the full content inside the `<LegalArticle>` slot). Add frontmatter:

```markdown
---
title: "Privacy Policy"
description: "Privacy Policy for 3AT - Endless Paradox: Discord data, MongoDB, Redis, XP, economy, and how to contact us."
lastUpdated: "April 8, 2026"
---

This Privacy Policy explains how the operators of **3AT - Endless Paradox** ("3AT," "we," "us") handle information...

[Copy the full content from the current privacy.astro <LegalArticle> slot, converting HTML to Markdown]
```

Note: The actual content is extensive. Copy all the `<p>`, `<h2>`, `<h3>`, `<ul>`, `<li>` content from `landing/src/pages/privacy.astro` (lines 12–232), convert to Markdown. Links stay as Markdown links: `[Discord's Privacy Policy](https://discord.com/privacy)`.

- [ ] **Step 2: Create terms.md**

Create `landing/src/content/pages/en/terms.md` with the same approach — frontmatter + Markdown content extracted from `terms.astro`.

```markdown
---
title: "Terms of Service"
description: "Terms of Service for 3AT - Endless Paradox: acceptable use, slash commands, third-party content, disclaimers."
lastUpdated: "April 8, 2026"
---

These Terms of Service ("Terms") govern your access to and use of **3AT - Endless Paradox**...

[Copy and convert from terms.astro]
```

- [ ] **Step 3: Add `pages` collection to content.config.ts**

Add the `pages` collection and update the `lastUpdated` field in the schema. Replace the entire file:

```ts
import { defineCollection } from "astro:content";
import { glob } from "astro/loaders";
import { z } from "astro/zod";

const commands = defineCollection({
  loader: glob({ pattern: "**/*.md", base: "./src/content/commands" }),
  schema: z.object({
    title: z.string(),
    command: z.string(),
    category: z.string(),
    description: z.string(),
    permissions: z.array(z.string()).optional(),
    cooldown: z.string().optional(),
  }),
});

const guides = defineCollection({
  loader: glob({ pattern: "**/*.md", base: "./src/content/guides" }),
  schema: z.object({
    title: z.string(),
    description: z.string(),
    icon: z.string(),
    order: z.number(),
    relatedCommands: z.array(z.string()).optional(),
  }),
});

const pages = defineCollection({
  loader: glob({ pattern: "**/*.md", base: "./src/content/pages" }),
  schema: z.object({
    title: z.string(),
    description: z.string().optional(),
    lastUpdated: z.string().optional(),
  }),
});

export const collections = { commands, guides, pages };
```

- [ ] **Step 4: Refactor privacy.astro to render from collection**

Replace the entire `landing/src/pages/privacy.astro`:

```astro
---
import { getCollection, render } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import LegalArticle from "../components/LegalArticle.astro";

const allPages = await getCollection("pages");
const page = allPages.find((p) => p.id === "en/privacy")!;
const { Content } = await render(page);
---

<BaseLayout title={`${page.data.title} | 3AT - Endless Paradox`} description={page.data.description}>
  <LegalArticle title={page.data.title} lastUpdated={page.data.lastUpdated ?? ""}>
    <Content />
  </LegalArticle>
</BaseLayout>
```

- [ ] **Step 5: Refactor terms.astro to render from collection**

Replace the entire `landing/src/pages/terms.astro`:

```astro
---
import { getCollection, render } from "astro:content";
import BaseLayout from "../layouts/BaseLayout.astro";
import LegalArticle from "../components/LegalArticle.astro";

const allPages = await getCollection("pages");
const page = allPages.find((p) => p.id === "en/terms")!;
const { Content } = await render(page);
---

<BaseLayout title={`${page.data.title} | 3AT - Endless Paradox`} description={page.data.description}>
  <LegalArticle title={page.data.title} lastUpdated={page.data.lastUpdated ?? ""}>
    <Content />
  </LegalArticle>
</BaseLayout>
```

- [ ] **Step 6: Verify build passes**

Run: `cd landing && npm run build`
Expected: Build succeeds. `/privacy` and `/terms` render from Markdown content.

- [ ] **Step 7: Commit**

```bash
git add landing/src/content/pages/ landing/src/content.config.ts landing/src/pages/privacy.astro landing/src/pages/terms.astro
git commit -m "feat(landing): move privacy/terms to Markdown content collection

Extract legal page content from hardcoded Astro to Markdown files
in content/pages/en/. Add pages collection to content.config.ts.
Privacy and terms pages now render from the collection."
```

---

### Task 11: Dynamic Route Pages — Add i18n

**Files:**
- Modify: `landing/src/pages/[lang]/commands/[...slug].astro`
- Modify: `landing/src/pages/[lang]/guide/[...slug].astro`

- [ ] **Step 1: Add t() to commands detail page**

In `landing/src/pages/[lang]/commands/[...slug].astro`, add i18n imports after the existing imports:

```astro
import { useTranslations } from "../../../i18n/utils";
import type { Lang } from "../../../i18n/utils";
```

After `const { lang, slug } = Astro.params;`, add:

```ts
const t = useTranslations(lang as Lang);
```

Replace hardcoded strings in the template:

- `<a href="/commands" class="guide-back-link">← Back to Commands</a>` → `<a href="/commands" class="guide-back-link">{t("commands.back")}</a>`
- In `breadcrumbItems`, replace `{ label: "Commands", href: "/commands" }` → `{ label: t("nav.commands"), href: "/commands" }`
- The translation notice: `Bản dịch đang được cập nhật...` → `{t("translation.notice")}`

Also pass `lang` to BaseLayout: `<BaseLayout title={pageTitle} description={page.data.description} hreflang={hreflang} lang={lang as Lang}>`

- [ ] **Step 2: Add t() to guide detail page**

In `landing/src/pages/[lang]/guide/[...slug].astro`, add i18n imports:

```astro
import { useTranslations } from "../../../i18n/utils";
import type { Lang } from "../../../i18n/utils";
```

After `const { lang, slug } = Astro.params;`, add:

```ts
const t = useTranslations(lang as Lang);
```

Replace hardcoded strings:

- `<a href="/guide" class="guide-back-link">← Back to Guide</a>` → `<a href="/guide" class="guide-back-link">{t("guide.back")}</a>`
- In `breadcrumbItems`, replace `{ label: "Guide", href: "/guide" }` → `{ label: t("nav.guide"), href: "/guide" }`
- The translation notice: `Bản dịch đang được cập nhật...` → `{t("translation.notice")}`
- `<h2 class="related-title">Related Commands</h2>` → `<h2 class="related-title">{t("guide.related")}</h2>`

Also pass `lang` to BaseLayout: `<BaseLayout title={pageTitle} description={page.data.description} hreflang={hreflang} lang={lang as Lang}>`

- [ ] **Step 3: Verify build passes**

Run: `cd landing && npm run build`
Expected: Build succeeds. All `/en/commands/*` and `/vi/commands/*` and `/en/guide/*` and `/vi/guide/*` routes generate correctly.

- [ ] **Step 4: Commit**

```bash
git add landing/src/pages/[lang]/
git commit -m "feat(landing): add i18n to dynamic command and guide detail pages

Replace hardcoded 'Back to Commands', 'Back to Guide', breadcrumb
labels, translation notices, and 'Related Commands' heading with
t() calls. Pass lang to BaseLayout for correct <html lang>."
```

---

### Task 12: Final Build Verification + Cleanup

**Files:**
- No new files

- [ ] **Step 1: Full clean build**

Run: `cd landing && rm -rf dist .astro && npm run build`
Expected: Build succeeds with no errors or warnings.

- [ ] **Step 2: Verify route generation**

Run: `ls landing/dist/vi/` to confirm Vietnamese pages exist.
Expected output should include: `index.html`, `commands/index.html`, `guide/index.html`

Run: `ls landing/dist/privacy/` and `ls landing/dist/terms/` to confirm legal pages still work.

- [ ] **Step 3: Verify no dangling faq.ts imports**

Run: `grep -r "faq.ts\|from.*data/faq" landing/src/`
Expected: No results (faq.ts was deleted and all imports removed)

- [ ] **Step 4: Commit if any cleanup was needed**

Only if previous steps revealed issues that needed fixing:

```bash
git add -A landing/
git commit -m "fix(landing): cleanup after i18n implementation"
```
